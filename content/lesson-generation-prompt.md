# Промпт для черновика темы (математика) через дешёвую модель

Как использовать:
1. Возьми название темы и главы из `content/curriculum/math-grade-N*.json`.
2. Скопируй текст ниже, замени всё в `{{…}}` на конкретные значения.
3. Отправь в дешёвую модель на OpenRouter (например `deepseek/deepseek-chat` или `qwen/qwen-2.5-72b-instruct` — они хорошо держат JSON и дешевле GPT/Claude).
4. Результат — JSON с полями `blocks` и `questions` — пришли мне. Я проверю математику, язык и уровень сложности, оберну в полный seed-файл с правильными slug/order и импортирую.

Не проси модель сразу выдавать полный seed-файл (с subject/class/chapter/slug/order) — эти поля механические, их проставляю я сам, чтобы не было ошибок нумерации.

---

## Промпт (копировать всё ниже черты)

Ты — автор учебного контента для Telegram-приложения по подготовке к вступительному экзамену в Узбекистане («30 ball.uz»). Аудитория — школьники 15–18 лет, которые претендуют на сильный результат, поэтому им не интересны голые определения — им нужна конкретная техника решения задач.

Тема: **{{НАЗВАНИЕ ТЕМЫ}}**
Класс: **{{КЛАСС}}**, глава: **{{НАЗВАНИЕ ГЛАВЫ}}**

### Жёсткие правила содержания

1. Никогда не копируй текст из учебников дословно — пиши с нуля, опираясь на саму математику.
2. В основе темы — один или два полностью разобранных числовых примера с конкретными числами, а не абстрактные формулировки. Каждый шаг решения объясняется словами «почему так», а не просто «делаем так».
3. Обязательно покажи технику, которая реально встречается в тестовых заданиях этого уровня (перебор случаев, формула, алгоритм действий) — не только определение термина.
4. Никаких терминов без объяснения тут же на месте. Не используй термин, пока не объяснил его на конкретном примере с числами. Если термин можно заменить простыми словами — замени.
5. Никогда не выдумывай факты, даты, формулы — если не уверен в формуле, не включай её.
6. Пиши по-русски, коротко и по делу, без канцелярита и воды.
7. Если тема связана с чем-то визуальным или пространственным (шкала, ось, фигура, расположение объектов) — обязательно покажи текстовую схему прямо в тексте, например: `0 — 5 — 10 — 15 — 20` для шкалы с шагом 5. Не объясняй пространственные вещи только словами без наглядного примера.
8. Примеры и задачи располагай от простого к сложному: первый разбор (`steps`) — самая прямая, лёгкая формулировка (дано известное — найди то, что сразу считается по этому известному). Только следующий пример может быть сложнее или «обратным» (дано следствие — найди исходное). Никогда не начинай тему со сложного или обратного примера.
9. Блок `text` в самом начале — это ПОЛНОЦЕННОЕ введение в тему с нуля, а не продолжение чужой мысли. Первое предложение должно прямо называть, что это за понятие/техника, будто читатель никогда о ней не слышал. Никогда не начинай с фраз вроде «В тестах не проверяют...», «Также важно...», «Не спеши считать...» — это звучит как середина текста. Сначала объясни, ЧТО это, и только потом — зачем это нужно в тестах.
10. Тебе может быть дан список техник/подтем ("покрытие"), которые реально относятся к этой теме по программе. Если он дан — ОБЯЗАТЕЛЬНО заведи отдельный блок (`steps`, `rule` или `decisionTree`) под каждую технику из списка, а не только под одну-две. Тема должна покрывать все перечисленные техники, а не быть однобокой.

### Формат ответа

Ответь ТОЛЬКО валидным JSON, без markdown-обёртки (без ```), без комментариев до или после. Структура ровно такая:

```json
{
  "blocks": [ /* массив блоков, см. допустимые типы ниже */ ],
  "questions": [ /* массив вопросов, см. формат ниже */ ]
}
```

### Допустимые типы блоков (используй столько блоков, сколько нужно, чтобы покрыть все обязательные техники (обычно 6-10), разных типов, в разумном порядке: сначала text, потом разбор примера, потом правило, потом опционально decisionTree, в конце flashcards)

**text** — вводный абзац(ы) с объяснением понятия. Можно подсветить ключевые слова:
```json
{ "type": "text", "paragraphs": [
  [ { "text": "обычный текст " }, { "text": "важное слово", "highlight": true }, { "text": " ещё текст." } ]
] }
```

**rule** — правило/формула одной строкой + примеры применения:
```json
{ "type": "rule", "rule": "формулировка правила или формулы", "examples": [
  "пример применения 1", "пример применения 2", "пример применения 3"
] }
```

**steps** — разбор одного конкретного примера пошагово. `condition` — это условие задачи (рендерится отдельным блоком):
```json
{ "type": "steps", "condition": "Условие конкретной задачи с числами", "steps": [
  { "text": "Что делаем на этом шаге", "explanation": "Почему именно так, с конкретными числами" },
  { "text": "...", "explanation": "..." }
] }
```

**decisionTree** — дерево выбора метода в зависимости от того, что спрашивают в задаче (используй, если у темы есть 2-3 разных сценария задач):
```json
{ "type": "decisionTree", "root": {
  "question": "Что нужно найти в задаче?",
  "options": [
    { "label": "Сценарий 1", "next": { "method": "как решать", "example": "конкретный пример с числами" } },
    { "label": "Сценарий 2", "next": { "method": "как решать", "example": "конкретный пример с числами" } }
  ]
} }
```

**flashcards** — 4–6 карточек вопрос/ответ для повторения:
```json
{ "type": "flashcards", "cards": [
  { "question": "Короткий вопрос", "answer": "Короткий точный ответ" }
] }
```

**linkedFormula** (опционально, если тема про формулу с несколькими переменными):
```json
{ "type": "linkedFormula", "problem": "текст условия", "formula": "формула",
  "links": [ { "id": "a", "problemLabel": "часть текста условия", "formulaLabel": "часть формулы" } ] }
```

**firstStep** (опционально, для задачи «с чего начать»):
```json
{ "type": "firstStep", "problem": "условие задачи",
  "firstChoices": [ { "label": "вариант первого действия", "correct": true, "feedback": "почему верно/неверно" } ],
  "secondChoices": [ { "label": "вариант второго действия", "correct": true, "feedback": "почему верно/неверно" } ] }
```

### Формат вопросов (6–8 штук, минимум по одному на каждую обязательную технику, `order` — по порядку с 0)

```json
{ "text": "Текст вопроса", "options": ["вариант 1", "вариант 2", "вариант 3", "вариант 4"],
  "correctIdx": 0, "explanation": "Почему именно этот ответ верный", "order": 0 }
```

`correctIdx` — это индекс (с нуля) правильного варианта в массиве `options`. Варианты должны быть правдоподобными (не абсурдными), 3–4 варианта на вопрос. Вопросы должны проверять именно технику из блока `steps`/`decisionTree`, а не только определение.

### Пример готовой темы такого же уровня (ориентир по глубине и стилю, тема "Натуральные числа и ноль", 5 класс)

```json
{
  "blocks": [
    {
      "type": "text",
      "paragraphs": [
        [
          { "text": "Натуральные числа — это " },
          { "text": "1, 2, 3, 4, 5", "highlight": true },
          { "text": " и так далее: целые и положительные. После любого числа есть следующее, поэтому натуральных чисел бесконечно много." }
        ],
        [
          { "text": "Отдельно стоит " },
          { "text": "0", "highlight": true },
          { "text": " — ноль. Он меньше любого натурального числа, но сам в их число не входит: ноль означает, что предметов нет." }
        ],
        [
          { "text": "В задачах часто пишут: " },
          { "text": "«a и b — натуральные числа»", "highlight": true },
          { "text": ". Это подсказка: если известно произведение a · b, оба числа можно найти без перебора всех чисел подряд — только через " },
          { "text": "делители", "highlight": true },
          { "text": " (делитель — это число, на которое данное число делится без остатка)." }
        ]
      ]
    },
    {
      "type": "rule",
      "rule": "Если a · b = N, а a и b — натуральные числа, то все пары (a; b) — это все способы разложить N на два делителя.",
      "examples": [
        "a · b = 12 → пары (a; b): (1;12), (2;6), (3;4), (4;3), (6;2), (12;1)",
        "a · b = 7 → пары только (1;7) и (7;1), потому что у числа 7 нет других делителей",
        "0 в такие пары никогда не входит: 0 не натуральное число"
      ]
    },
    {
      "type": "steps",
      "condition": "Пример: a · b = 24, найти наименьшую возможную сумму a + b",
      "steps": [
        { "text": "Находим все делители числа 24", "explanation": "1, 2, 3, 4, 6, 8, 12, 24 — каждое из них делит 24 без остатка." },
        { "text": "Составляем из них все пары (a; b) с произведением 24", "explanation": "(1;24), (2;12), (3;8), (4;6), (6;4), (8;3), (12;2), (24;1)." },
        { "text": "Считаем сумму a + b для каждой пары", "explanation": "1+24=25, 2+12=14, 3+8=11, 4+6=10, 6+4=10, 8+3=11, 12+2=14, 24+1=25." },
        { "text": "Выбираем нужный ответ", "explanation": "Нужна была наименьшая сумма — среди всех посчитанных сумм это 10 (у пар 4 и 6, 6 и 4)." }
      ]
    },
    {
      "type": "decisionTree",
      "root": {
        "question": "Известно a · b = N, a и b — натуральные числа. Что нужно найти?",
        "options": [
          {
            "label": "Все возможные пары (a; b)",
            "next": {
              "method": "Найди все делители N — каждый делитель это a, вторая часть пары b = N ÷ a.",
              "example": "N = 20: делители 1, 2, 4, 5, 10, 20 → пары (1;20), (2;10), (4;5), (5;4), (10;2), (20;1)"
            }
          },
          {
            "label": "Наибольшую или наименьшую сумму a + b",
            "next": {
              "method": "Составь все пары, посчитай сумму a + b для каждой и сравни числа между собой.",
              "example": "N = 18: пары и суммы — (1;18)→19, (2;9)→11, (3;6)→9. Наибольшая сумма — 19, наименьшая — 9"
            }
          }
        ]
      }
    },
    {
      "type": "flashcards",
      "cards": [
        { "question": "Что такое натуральное число?", "answer": "Целое положительное число: 1, 2, 3, 4 и так далее." },
        { "question": "Входит ли ноль в натуральные числа?", "answer": "Нет. Ноль меньше любого натурального числа, но в их число не входит." },
        { "question": "Зачем в задачах уточняют «a и b — натуральные числа»?", "answer": "Чтобы искать ответ через делители произведения, а не перебором всех чисел подряд." }
      ]
    }
  ],
  "questions": [
    {
      "text": "Какое из чисел является натуральным?",
      "options": ["0", "-2", "7", "0,5"],
      "correctIdx": 2,
      "explanation": "7 — целое и больше нуля, им можно посчитать предметы. 0, отрицательные и дробные числа натуральными не являются.",
      "order": 0
    },
    {
      "text": "Для натуральных чисел a и b верно a · b = 24. Найдите наименьшее возможное значение a + b.",
      "options": ["10", "11", "14", "25"],
      "correctIdx": 0,
      "explanation": "Пары числа 24 и их суммы: 25, 14, 11, 10, 10, 11, 14, 25. Наименьшая — 10, у пар (4;6) и (6;4).",
      "order": 1
    }
  ]
}
```

(это сокращённый пример — в реальной теме 1 было 5 блоков и 6 вопросов; ориентируйся на этот же уровень подробности и тот же стиль языка)

---

Конец промпта.
