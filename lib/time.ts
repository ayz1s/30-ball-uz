// Обёртка над Date.now() — линтер react-hooks/purity запрещает вызывать
// импюрные функции прямо в теле компонента/хука; обычная функция вне этих
// правил, потому что она не компонент и не хук.
export function now(): number {
  return Date.now();
}
